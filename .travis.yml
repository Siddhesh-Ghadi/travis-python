arch: ppc64le
jobs:
  include:
    - dist: bionic
    - dist: xenial
  
script:
  - git config --list
  - ls -la /home/travis/.cache
  - ls -la /home/travis/.cache/pip
  - ls -la /home/travis/.cache/pip/wheels
  
